{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["svg", "notes"],
  "properties": {
    "svg": { "type": "string", "description": "完整的 <svg>…</svg>，可直接嵌入 HTML" },
    "notes": { "type": "string", "description": "畫了什麼、用了哪些手法，兩三句" }
  }
}
