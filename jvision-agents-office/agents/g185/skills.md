# 技能實例（行銷排程 專屬）

> 規則見 POLICY.md 與 datamodes/internal-sim.md；以下為本 Agent 對各技能的領域特化。

## 資源排程
- 依 行銷推廣 情境運用「資源排程」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。

## 任務分派
- 依 行銷推廣 情境運用「任務分派」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。

## 衝突偵測
- 依 行銷推廣 情境運用「衝突偵測」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。

## 關鍵路徑
- 依 行銷推廣 情境運用「關鍵路徑」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。

## 甘特圖產出
- 依 行銷推廣 情境運用「甘特圖產出」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。

## 負載平衡
- 依 行銷推廣 情境運用「負載平衡」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。
