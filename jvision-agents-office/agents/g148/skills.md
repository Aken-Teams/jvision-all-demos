# 技能實例（資產填料 專屬）

> 規則見 POLICY.md 與 datamodes/internal-sim.md；以下為本 Agent 對各技能的領域特化。

## 擬真資料生成
- 依 資產管理 情境運用「擬真資料生成」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。

## 情境樣本
- 依 資產管理 情境運用「情境樣本」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。

## 邊界案例
- 依 資產管理 情境運用「邊界案例」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。

## 空資料案例
- 依 資產管理 情境運用「空資料案例」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。

## 資料遮罩
- 依 資產管理 情境運用「資料遮罩」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。

## 分布校準
- 依 資產管理 情境運用「分布校準」，遵守 dataMode：internal-sim（詳見 ../../datamodes/internal-sim.md）。
